# M6 — Visual và provenance, triển khai theo lát

Ngày 13-09-2026. Nguồn ràng buộc: đặc tả A–Z §7, §13, §21, §23; dùng lại
`AssetManifest`, medical/video gate và staging hiện có. Mỗi lát có test offline,
review và commit riêng. Không suy diễn số liệu y khoa hoặc giấy phép.

| Lát | Phạm vi | Tiêu chí kiểm chứng |
| --- | --- | --- |
| M6.1 | Chart SVG tĩnh từ datum số đã ghi rõ trong evidence ledger; provenance/quyền sử dụng explicit và `data_chart` semantic trong manifest hiện có | Cùng input cho cùng bytes; trục/đơn vị/mẫu số/CI hiển thị; nguồn–claim hợp lệ; hash asset và ledger khớp; medical gate bắt đổi bytes; không đổi state/v1 |
| M6.2 | Paper highlight từ ảnh nguồn cục bộ và tọa độ/câu trích/marker do operator cung cấp | Crop/quote/marker nhất quán; lưu tọa độ, trang, nguồn và quyền dùng; không lộ full text; đổi bytes làm gate stale |
| M6.3 | SVG whiteboard và mascot rig cố định | `mascot_reaction` chỉ trang trí; annotation y khoa luôn semantic; ảnh tĩnh xác định, Remotion dùng asset đã khai báo |
| M6.4 | Hook ở frame 0, không intro; outro là dòng script và scene cuối đã duyệt, dùng brand asset khai báo | Không giới hạn tổng thời lượng v2; TTS khớp timeline; v1, hai gate và đăng thủ công không đổi. Xem đặc tả `2026-09-13-pyh-hook-outro-m6-4-design.md` |
| M6.5 | Tích hợp render và QA tỷ lệ visual | 65–75% whiteboard/SVG, 15–25% chart/crop, AI clip ≤10% nếu không có chỉ định khác; 1080×1920/30 fps, promotion và hai gate còn nguyên |
| M6.6 | Veo tùy chọn, sau khi xác minh API/quota/quyền hiện hành | Chỉ opt-in; fixture/fake transport trong CI; clip được ghi provenance và phân loại semantic; không chặn pipeline khi thiếu Veo |

M6.1 chỉ đọc datum `count_of_total` có `value`, `denominator`, `unit`, nhãn,
source ID, CI tùy chọn và claim ID bao quanh đã được ghi trước trong ledger.
Không tính ra tỷ lệ hay làm tròn mệnh đề. SVG chỉ thể hiện thang 0–mẫu số, giá
trị gốc và CI nếu có. Cần nguồn tồn tại và thuộc claim; quyền sử dụng/giấy phép
do operator cung cấp bằng chuỗi tường minh, code chỉ xác thực có/khớp, không
kết luận quyền pháp lý. Workflow chỉ hoạt động trên revision v2 chưa được duyệt,
ghi SVG + asset manifest + license ledger trong một lần promotion thư mục assets,
không đổi state. Không sửa Remotion hoặc v1 ở lát này.

Test RED trước: chart thiếu/không hữu hạn/vượt mẫu số/CI sai; SVG lặp byte và
không có external reference/script; license ledger path/hash/nguồn/quyền/duplicate;
workflow từ fixture synthetic tạo asset semantic, metadata khớp, gate từ chối
bytes bị sửa, không ghi project.yaml, không ghi đè asset cũ. Sau GREEN chạy suite
Python, Ruff, schema export/diff, 12 video tests/typecheck, `git diff --check`.
